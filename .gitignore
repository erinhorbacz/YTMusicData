# OS noise
.DS_Store

# Dependencies
node_modules/

# Build output
YTMusicApp/build/

# Server secrets & runtime data
server/.env
server/data/*
!server/data/.gitkeep

# Personal listening data. NOTE: the root watch-history.json is deliberately
# COMMITTED — it's the public site's default dataset (owner's choice).
listening_data.csv
Takeout/
*.zip
